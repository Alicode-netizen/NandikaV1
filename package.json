
{
  "name": "nandikav1",
  "version": "0.1.0",
  "private": true,
  "description": "Short description of the project",
  "main": "src/index.js",
  "scripts": {
    "dev": "vite",                /* or `react-scripts start` or `next dev` depending on framework */
    "start": "npm run dev",
    "build": "vite build",        /* or `react-scripts build` or `next build` */
    "preview": "vite preview",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "format": "prettier --write .",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    /* keep runtime dependencies here */
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^2.0.0"
  }
}
